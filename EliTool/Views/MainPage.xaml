<Page
    x:Class="EliTool.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AppUIBasics.Controls" xmlns:data="using:EliTool.DataModels"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <!--<ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Controls/ControlItemTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>-->
            <DataTemplate x:Name="ControlItemInfoTemplate" x:DataType="data:ControlInfoDataItem">
                <UserControl>
                    <Grid
                        x:Name="controlRoot"
                        Width="360"
                        Height="90"
                        Padding="8"
                        HorizontalAlignment="Stretch"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="LayoutVisualStates">
                                <VisualState x:Name="WideLayout">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint640Plus}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters />
                                </VisualState>
                                <VisualState x:Name="NarrowLayout">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="0" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="controlRoot.Width" Value="Auto" />
                                        <Setter Target="controlRoot.Height" Value="120" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid Width="72" CornerRadius="{ThemeResource ControlCornerRadius}">
                            <Image
                                x:Name="gridImage"
                                Width="48"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="{x:Bind Title}"
                                Source="{x:Bind ImagePath}"
                                Stretch="Uniform" />
                        </Grid>
                        <StackPanel
                                Grid.Column="1"
                                Grid.ColumnSpan="2"
                                Margin="16,0,0,0"
                                VerticalAlignment="Center"
                                Orientation="Vertical"
                                Spacing="2">
                            <TextBlock
                                    x:Name="titleText"
                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                    Text="{x:Bind Title}"
                                    TextLineBounds="TrimToCapHeight"
                                    TextWrapping="NoWrap" />
                            <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    RelativePanel.Below="titleText"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind Subtitle}"
                                    TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Grid>
                </UserControl>
            </DataTemplate>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <Color x:Key="HeaderBackgroundFirstStop">#FFCFD8E4</Color>
                    <Color x:Key="HeaderBackgroundLastStop">#00CFD8E4</Color>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <Color x:Key="HeaderBackgroundFirstStop">#FF000000</Color>
                    <Color x:Key="HeaderBackgroundLastStop">#00000000</Color>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid HorizontalAlignment="Stretch" Margin="-56,-24,-56,0" x:Name="ContentArea">
        <StackPanel HorizontalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <Grid Height="auto" HorizontalAlignment="Stretch">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <LinearGradientBrush.GradientStops>
                            <GradientStop Color="{ThemeResource HeaderBackgroundFirstStop}"/>
                            <GradientStop Color="{ThemeResource HeaderBackgroundLastStop}" Offset="1.0"/>
                        </LinearGradientBrush.GradientStops>
                    </LinearGradientBrush>
                </Grid.Background>
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="50">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Style="{StaticResource BodyTextBlockStyle}">ETool V1.0</TextBlock>
                            <TextBlock Style="{StaticResource TitleLargeTextBlockStyle}">Elipese Tool Gallery</TextBlock>
                        </StackPanel>
                    </Grid>
                <Image HorizontalAlignment="Right" Height="auto" Width="auto" Source="/Assets/Header.png" Stretch="None"/>
                <controls:TileGallery 
                        x:Name="headerTileGallery"
                        Grid.Row="1"
                        Margin="0,0,0,0"
                        VerticalAlignment="Bottom"
                        HorizontalAlignment="Stretch"/>
            </Grid>
            <GridView Margin="35" ItemTemplate="{StaticResource ControlItemInfoTemplate}" ItemsSource="{x:Bind ControlInfos}" ItemClick="GridView_ItemClick">
            </GridView>
        </StackPanel>
    </Grid>
</Page>
